def process(events):
    """Return `events` with duplicate ids removed, keeping the first occurrence
    of each id and preserving order."""
    raise NotImplementedError("process() is not implemented yet")
