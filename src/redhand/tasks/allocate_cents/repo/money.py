def split_evenly(total, n):
    """Split `total` cents into `n` parts summing exactly to `total`."""
    raise NotImplementedError("split_evenly() is not implemented yet")


def allocate(total, weights):
    """Allocate `total` cents proportionally to `weights`, summing exactly to `total`."""
    raise NotImplementedError("allocate() is not implemented yet")
